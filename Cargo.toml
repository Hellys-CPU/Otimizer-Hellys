[package]
name = "systemforge-shared"
version = "0.1.0"
edition = "2021"
description = "Protocolo IPC e allowlist de ações compartilhados entre o Core (src-tauri) e o Serviço privilegiado"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
thiserror = "1"
